# CH05（上）笔记卡 - 第九大区-袁大鹿

**作者**：第九大区-袁大鹿  
**提交时间**：2025-11-05 00:21:02

## 【课程笔记】

课程前半小时，仲伟大致解读了 MCP 开发教程。其中个人印象最深的有以下 2 个：

---

## 1、MCP 的核心概念（开发者视角）

### Client（客户端）

你日常使用的 AI 工具，如 Tree、ChatGPT。我们通常不开发它。

### Server（服务端）

你要开发的 MCP 应用，它为 Client 提供工具和能力。这是我们本节课构建的目标。

### Protocol（协议）

Client 和 Server 之间沟通的标准语言（基于 JSON）。

---

## 2、MCP 的三个核心组件

### Tools（工具）：可调用的函数

**类比**：餐厅的服务项目——"帮你加热"、"打包外带"、"切成小块"

**特点**：
- AI 可以主动调用
- 需要定义输入参数和返回值
- 用于执行操作（计算、查询、修改数据等）

---

### Resource（资源）：可读取的数据

**类比**：餐厅的菜单、今日特价表、店内装饰照片——顾客可以"看"，但不能"操作"

**特点**：
- AI 可以列举和读取
- 通常用 URI 标识（如 `file:///path/to/file`）
- 支持订阅（资源变化时主动通知 Client）
- 用于提供上下文信息

---

### Prompt（提示模板）：可复用的提示词

**类比**：餐厅的"套餐组合"——已经配好的搭配方案

**特点**：
- 预设的提示词模板
- 可以包含参数（动态填充）
- 帮助用户快速开始对话

---

## 实践部分

课程后面实操讲解，我会在实践练习中，按照老师演示的一步步实操。

